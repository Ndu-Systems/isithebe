export interface Policy {
    PolicyId: string;
    Description: string;
    Amount: string;
    CreateUserId: string;
    CreateDate: string;
    ModifyUserId: string;
    ModifyDate: string;
    StatusId: string;
}